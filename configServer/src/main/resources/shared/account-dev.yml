server:
  port: 8050
spring:
  application:
    name: account
  datasource:
    name: orderingsystem
    url: jdbc:mysql://localhost:3306/orderingsystem?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

#使用注解这里可以不用配
#mybatis:
#  mapper-locations: classpath:/mapping/*.xml
#  type-aliases-package: you.shaoxin.domin