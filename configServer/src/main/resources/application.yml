server:
  port: 8762
spring:
  application:
    name: configserver
  profiles:
    active: native  #从本地读取配置文件
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared  #指定配置文件的路径